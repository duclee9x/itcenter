# Artifact Repository

Owner of the `artifact` schema and immutable artifact identity, scan evidence,
review and lifecycle state. The module stores metadata and opaque object
references only; object, malware scanning and signature verification are
application ports. No provider is configured by default.
