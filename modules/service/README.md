# Service Reference Catalog

Feature: TASK-093-R2A
Workflow: Knowledge applicability reference foundation

Owns tenant-scoped Service, Platform and ServiceEnvironment reference data.
Other domains consume typed references and application query ports; they do
not write these tables. Platform observation text is resolved only by exact,
unique configured key/name matches. There is no fuzzy resolution or seeded OS
catalog.
