services:
  api:
    environment:
      APP_ENV: staging

  agent-gateway:
    environment:
      APP_ENV: staging
    ports: []

  worker:
    environment:
      APP_ENV: staging

  migrate:
    environment:
      APP_ENV: staging

  caddy:
    ports: []
