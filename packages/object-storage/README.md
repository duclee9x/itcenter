# object-storage

Optional metadata port. No provider infrastructure or binary persistence is enabled.

Owner: Platform. Task: TASK-000. Must not contain business-domain workflows or import domain modules.
